<html class="user">
<h1>Users</h1>

<p><a href="/new_user">Add a New User</a></p>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
  <% @users.each do |user| %>
    <tr>
          <td><%= user.name %></td>
         <% if session['uid'] %>
        <td><a href="/users/<%= user.id %>">email</a></td>
      <% end %>
          <td><a href="/users/<%= user.id %>">Show &nbsp</a>
        </td>    <% if session['uid'] %>
        <td><a href="/users/<%= user.id %>/edit">Edit</a></td>
        <td><a href="/users/<%= user.id %>/destroy">Destroy</a></td>
      <% end %>
    </tr>
  <% end %>
  </tbody>
</table>

